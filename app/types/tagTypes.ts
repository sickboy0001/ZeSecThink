export interface TypeTagMas {
  id: string;
  tagName: string;
  name: string;
  description: string;
  visible_flg: boolean;
  favorite_flg: boolean;
  create_at?: string; // DateTime
  update_at?: string; // DateTime
}
